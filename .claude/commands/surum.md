---
description: STATUS.md + README sayılarını ve commit'i güncelle
---

`surum-guncelle` skill'ini çağır ve checklist'ini uygula.

Tool/test sayılarını `grep`/`uv run pytest` çıktısından doğrula (tahmin etme); STATUS.md
`Son güncelleme:` tarihini bugüne çek; commit gövdesine `Co-Authored-By` trailer'ı ekle.
