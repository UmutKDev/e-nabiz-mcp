---
description: parsers.py'ye yeni bir HTML→model parser ekle
---

`yeni-parser-ekle` skill'ini çağır ve desenini uygula. Parser'ı yazılacak sayfa/endpoint:
$ARGUMENTS

Hatırlatma: tam tablo id (fallback yok), `_rows()`/`_cell()`, değerler `str`, eksik
yapıda `[]`.
